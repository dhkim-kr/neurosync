# React Native / Expo (Metro) does not resolve pnpm's default symlinked,
# isolated node_modules layout. Use a flat (hoisted) layout so Metro can
# resolve transitive deps like @babel/runtime. See Expo monorepo docs.
node-linker=hoisted
